{
    "scripts": {
        "start": "clear; killall ./bin/olist-api; go build -o bin/ && ./bin/olist-api",
        "watch": "./node_modules/.bin/nodemon --exec 'yarn start'"
    },
    "devDependencies": {
        "nodemon": "^2.0.13"
    }
}
